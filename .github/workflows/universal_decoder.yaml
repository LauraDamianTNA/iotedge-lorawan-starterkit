name: Universal Decoder CI
on: [push] # rebuild any PRs and main branch changes
  # pull_request:
  # push:
  #   branches:
  #     - master
  #     - dev

jobs:
  Build-And-Test:
    name: Build and Test Solution
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: Run Tests
        working-directory: ./Samples/UniversalDecoder/modules/UniversalDecoder
        run: |
          npm install
          echo "./Samples/UniversalDecoder/modules/UniversalDecoder/node_modules/.bin" >> $GITHUB_PATH
          echo "$GITHUB_PATH"
          npm install --save-dev jest
          npm run codecs
          npm test

      - name: Retrieve secret
        env:
          super_secret: ${{ secrets.GITSECRET }}
        run: |
          ls "$super_secret"
      - run: echo "🍏 This job's status is ${{ job.status }}."


